import {
  cilApplicationsSettings,
  cilMenu,
  cilSpeedometer,
  cilHome,
  cilInstitution,
  cilUser,
  cilStorage
} from '@coreui/icons';

export const iconSubset = {
  cilApplicationsSettings,
  cilMenu,
  cilSpeedometer,
  cilHome,
  cilInstitution,
  cilUser,
  cilStorage
};

export enum IconSubset {
  cilApplicationsSettings = 'cilApplicationsSettings',
  cilMenu = 'cilMenu',
  cilSpeedometer = 'cilSpeedometer',
  cilHome = 'cilHome',
  cilInstitution = 'cilIstitution',
  cilUser = 'cilUser',
  cilStorage = 'cilStorage'
}
